{% extends '@ConventionGeekMain/Structures/menu.html.twig' %}

{% block metaKeyword %}calendier, salon, jeux, vidéo, jeux vidéo, dates, convention, geek, otaku, japan, expo, France, free, manga, évenement{% endblock %}
{% block metaDescription %}Ceci est la page où vous prouvez retrouver tous les évènements geek qui aurons lieu les prochains mois{% endblock %}
{% block metaTitle %}Convention Geek: Conventions{% endblock %}

{% block pageContent %}

        <div class="mdc-layout-grid__inner mdc-elevation--z12">
            <div class="mdc-layout-grid__cell mdc-layout-grid__cell--span-12">

                <h2 style="text-align: center;">Prochainement</h2>

                <ul   class="mdc-list mdc-list--two-line" aria-orientation="vertical">

                    {% for event in listEvenement %}


                        <li class="mdc-list-item mdc-elevation--z1" >
                            <a class="a-list-item" href="{{ path('CGEvent_event', {'eventid': event.eventid}) }}">
                                <span class="mdc-list-item__text">
                                    <span class="mdc-list-item__primary-text">{{ event.name }} - {{ event.place }}</span>
                                    <span class="mdc-list-item__secondary-text">{{ event.date }}</span>

                                </span>
                                <span class="mdc-list-item__meta" aria-hidden="true">{{ event.informateur }}</span>
                            </a>
                        </li>

                    {% endfor %}
                </ul>

            </div>
            <div class="mdc-layout-grid__cell mdc-layout-grid__cell--span-12" style="display: flex;">
                <div class="section-spacer" style="flex-grow: 1;"></div>
                     <a href="{{ path('CGEvent_formulaires') }}" class="mdc-button mdc-button--raised">
                         Proposer une convention
                     </a>
            </div>
        </div>


{% endblock %}
